# Map Pinboard (React + TypeScript + Vite)

Live Url
https://map-pinboard-taupe.vercel.app/

* `useMapPins` hook handles mocking of pin list data, removing of pins, and default data values
* `MapBoard` is the main component that host maps